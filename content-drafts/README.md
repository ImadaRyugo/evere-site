# content-drafts — レビュー待ちの下書き

執筆済み・**ユーザーレビュー待ち**の記事を置くフォルダ（ビルド対象外）。

- 1記事 = 1ディレクトリ（`<slug>/{lang}.md`）。日付プレフィックスはまだ付けない
- レビューで承認されたら `content-queue/YYYY-MM-DD-<slug>/` へ移動する
  （日付 = 公開したい順序。実際の公開日は publish-queue.mjs が自動設定）
- 却下・保留の記事はここに残すか削除する
